angular.module('personal_record.directives.workout', [])/*
    .directive('totalTonnage', function () {
        return {
            restrict: 'E',
            scope: {
                //total_tonnage: '='
            },
            link: function (scope, element, attrs) {
                scope.total_tonnage = 0;

                console.log('wurrkouts' + scope.workouts);

                for(workout in scope.workouts) {
                    scope.total_tonnage += workout.weight;
                }
            },
            template: "{{ total_tonnage }} asdf"
        };
    });*/
